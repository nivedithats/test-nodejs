const { createResource, getAllResources, getResourceById, updateResource, deleteResource, getResourcesByProperty, searchResources } = require('./src/controllers');
const createModal = require("./src/CreateModal");
const mongoConnecter = require("./src/MongoConnecter");

module.exports = {
    createResource,
    getAllResources,
    getResourceById,
    updateResource,
    deleteResource,
    getResourcesByProperty,
    searchResources,
    createModal,
    mongoConnecter,
};