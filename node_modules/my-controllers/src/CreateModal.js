const mongoose = require('mongoose');

const createModal = (model, collection) =>{
    const Schema = new mongoose.Schema(model);

    return mongoose.model(collection, Schema);
}
module.exports = createModal;